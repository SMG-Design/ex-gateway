runtime: nodejs
env: flex

network:
  name: default

manual_scaling:
  instances: 1

resources:
  memory_gb: 4

env_variables:
  EXAUTH: $EXAUTH
  EXGATEWAYCACHEIP: $EXGATEWAYCACHEIP
  EXGATEWAYCACHEPORT: $EXGATEWAYCACHEPORT
  SOURCE: app-engine
